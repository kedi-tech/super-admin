# Phase 11 — Installation Module

## What was built

- `actions/installations.ts` — `getInstallations`
- `app/(admin)/super-admin/installations/page.tsx` — Server component with `force-dynamic`
- Table shows: license key, device ID, customer, installation date, status
- Read-only view; activations/deactivations are managed from the License detail page
